<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Aurora_LINK.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="24" Spacing="24" MaxWidth="560" HorizontalAlignment="Left">

            <!-- Titre -->
            <TextBlock Text="Tableau de bord"
                       FontSize="28"
                       FontWeight="SemiBold" />

            <!-- ═══ Scène active ═══ -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="20"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="16">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon Glyph="&#xE7BC;" FontSize="20"
                                  VerticalAlignment="Center" />
                        <TextBlock Text="Scène active"
                                   FontSize="18" FontWeight="SemiBold"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <StackPanel x:Name="ActiveScenePanel" Spacing="8">
                        <TextBlock x:Name="ActiveSceneStatus"
                                   Text="Aucune scène en auto-start"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ═══ Entrées physiques ═══ -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="20"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="16">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon Glyph="&#xE943;" FontSize="20"
                                  VerticalAlignment="Center" />
                        <TextBlock Text="Entrées physiques"
                                   FontSize="18" FontWeight="SemiBold"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <TextBlock x:Name="InputsConnectionStatus"
                               Text="En attente de connexion…"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                    <Grid x:Name="InputsGrid"
                          ColumnSpacing="8" RowSpacing="8"
                          Visibility="Collapsed">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
