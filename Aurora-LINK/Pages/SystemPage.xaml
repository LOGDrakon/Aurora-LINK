<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Aurora_LINK.Pages.SystemPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="24" Spacing="24" MaxWidth="560" HorizontalAlignment="Left">

            <!-- Titre -->
            <TextBlock Text="Système"
                       FontSize="28"
                       FontWeight="SemiBold" />

            <!-- ═══ Informations Appareil ═══ -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="20"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="16">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon Glyph="&#xE950;" FontSize="20"
                                  VerticalAlignment="Center" />
                        <TextBlock Text="Informations Appareil"
                                   FontSize="18" FontWeight="SemiBold"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <Grid ColumnSpacing="16" RowSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Modèle -->
                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="Modèle"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   VerticalAlignment="Center" />
                        <TextBlock Grid.Row="0" Grid.Column="1"
                                   x:Name="DeviceModel"
                                   Text="—"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center" />

                        <!-- Firmware -->
                        <TextBlock Grid.Row="1" Grid.Column="0"
                                   Text="Version firmware"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   VerticalAlignment="Center" />
                        <TextBlock Grid.Row="1" Grid.Column="1"
                                   x:Name="DeviceFirmware"
                                   Text="—"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center" />

                        <!-- UID -->
                        <TextBlock Grid.Row="2" Grid.Column="0"
                                   Text="UID"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   VerticalAlignment="Center" />
                        <TextBlock Grid.Row="2" Grid.Column="1"
                                   x:Name="DeviceUid"
                                   Text="—"
                                   FontFamily="Consolas"
                                   IsTextSelectionEnabled="True"
                                   VerticalAlignment="Center" />

                        <!-- Port série -->
                        <TextBlock Grid.Row="3" Grid.Column="0"
                                   Text="Port"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   VerticalAlignment="Center" />
                        <TextBlock Grid.Row="3" Grid.Column="1"
                                   x:Name="DevicePort"
                                   Text="—"
                                   VerticalAlignment="Center" />
                    </Grid>
                </StackPanel>
            </Border>

            <!-- ═══ Sécurité ═══ -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="20"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="16">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon Glyph="&#xE72E;" FontSize="20"
                                  VerticalAlignment="Center" />
                        <TextBlock Text="Sécurité"
                                   FontSize="18" FontWeight="SemiBold"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <TextBlock Text="Gérez le mot de passe de protection de l'appareil."
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextWrapping="Wrap" />

                    <!-- Mot de passe actuel -->
                    <PasswordBox x:Name="CurrentPasswordBox"
                                 Header="Mot de passe actuel"
                                 PlaceholderText="Entrez le mot de passe actuel" />

                    <!-- Nouveau mot de passe -->
                    <PasswordBox x:Name="NewPasswordBox"
                                 Header="Nouveau mot de passe"
                                 PlaceholderText="Entrez le nouveau mot de passe" />

                    <!-- Confirmation -->
                    <PasswordBox x:Name="ConfirmPasswordBox"
                                 Header="Confirmer le mot de passe"
                                 PlaceholderText="Confirmez le nouveau mot de passe" />

                    <Button x:Name="BtnChangePassword"
                            Content="Modifier le mot de passe"
                            Click="BtnChangePassword_Click"
                            Style="{ThemeResource AccentButtonStyle}"
                            HorizontalAlignment="Right" />
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
